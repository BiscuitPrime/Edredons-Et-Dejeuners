{% extends 'base.html.twig' %}

{% block title %}Region{% endblock %}

{% block body %}
    <h1>Region</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ region.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ region.name }}</td>
            </tr>
            <tr>
                <th>Presentation</th>
                <td>{{ region.presentation }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ region.country }}</td>
            </tr>
        </tbody>
    </table>

    <div align='center'>
        <br>
        <a href="{{ path('region_index') }}" class='btn btn-outline-dark'>back to list</a>
        <br>
        <a href="{{ path('region_edit', {'id': region.id}) }}" class='btn btn-outline-success'>edit</a>
        <br>
        </div>

    {{ include('region/_delete_form.html.twig') }}
{% endblock %}
