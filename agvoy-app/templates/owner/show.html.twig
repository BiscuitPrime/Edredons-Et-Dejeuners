{% extends 'base.html.twig' %}

{% block title %}Owner{% endblock %}

{% block body %}
    <h1>Owner</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ owner.id }}</td>
            </tr>
            <tr>
                <th>Firstname</th>
                <td>{{ owner.firstname }}</td>
            </tr>
            <tr>
                <th>FamilyName</th>
                <td>{{ owner.familyName }}</td>
            </tr>
            <tr>
                <th>Adress</th>
                <td>{{ owner.adress }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ owner.country }}</td>
            </tr>
        </tbody>
    </table>

    <div align='center'>
        <br>
        <a href="{{ path('owner_index') }}" class='btn btn-outline-dark'>back to list</a>
        <br>
        <a href="{{ path('owner_edit', {'id': owner.id}) }}" class='btn btn-outline-success'>edit</a>
        <br>
        </div>

    {{ include('owner/_delete_form.html.twig') }}
{% endblock %}
